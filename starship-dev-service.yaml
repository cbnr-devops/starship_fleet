apiVersion: v1
kind: Service

metadata:
  name: starship-dev-service

  labels:
    app: starship-dev
    env: dev

spec:

  type: LoadBalancer

  selector:
    app: starship-dev

  ports:

  - name: http

    port: 80

    targetPort: 8000
